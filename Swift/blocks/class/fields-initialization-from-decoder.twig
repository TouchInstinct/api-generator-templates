{%- import '../../macroses/common.utils.twig' as utils -%}
    let container = try decoder.container(keyedBy: CodingKeys.self)
    {%- for field in fields %}
        {{ field.name }} = {{ utils.formatNullableOrOptional("try", field.nullable, field.optional) }} container.decode({{ utils.formatValueType(field.type.type) }}.self, forKey: .{{ field.name }})
    {%- endfor -%}
