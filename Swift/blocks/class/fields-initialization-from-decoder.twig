{%- import '../../macroses/common.utils.twig' as utils -%}
    let container = try decoder.container(keyedBy: CodingKeys.self)
    {{ utils.numberFormatterFrom(fields) -}}
    {% for field in fields %}
        {{ utils.decodeFromContainer(field) }}
    {%- endfor -%}
