{%- import '../../macroses/common.utils.twig' as utils -%}
        {%- for field in fields -%}
        {{ field.name }}: {{ utils.defaultValueForField(field) }}{%- if not (loop.last) %}, {% endif %}
{%- endfor -%}