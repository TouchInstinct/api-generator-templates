{%- import '../../utils.twig' as utils -%}
        {%- for field in fields %}
        {{ field.name }} = {{ utils.formatNullable('try', field.nullable) }} {{ utils.mappingFromMapForField(field) }}
{%- endfor -%}