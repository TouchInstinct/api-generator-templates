{%- import '../../macroses/common.utils.twig' as utils -%}
        {%- for field in fields %}
        {%- if field.optional %}
        if {{ field.name }} != nil {
            {{ utils.mappingToMapForField(field, classType, typeParameters) }}
        }
        {%- else %}
        {{ utils.mappingToMapForField(field, classType, typeParameters) }}
        {%- endif -%}
{%- endfor -%}