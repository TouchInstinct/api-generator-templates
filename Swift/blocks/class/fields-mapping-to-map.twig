{%- import '../../macroses/common.utils.twig' as utils -%}
        {%- for field in fields %}
        {%- if field.optional %}
        if {{ field.name }} != nil {
            {{ field.name }} >>> {{ utils.mappingToMapForField(field, classType) }}
        }
        {%- else %}
        {{ field.name }} >>> {{ utils.mappingToMapForField(field, classType) }}
        {%- endif -%}
{%- endfor -%}