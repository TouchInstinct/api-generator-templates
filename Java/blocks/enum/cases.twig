{%- import '../../utils.twig' as utils -%}
{%- for value in values %}
	/**
	 * {{ value.description }}
	 */
{%- if valuesTypes == "STRING" %}
    @JsonStringValue("{{ value.value }}")
{%- elseif valuesTypes == "INT" %}
    @JsonNumberValue({{ value.value }})
{%- endif %}
    {{ value.nameUpperCaseWithUnderscope }}{%- if not (loop.last) %},{% else %};{% endif %}
{%- endfor -%}
